# REACT_APP_API_BASE_URL (optional). When unset, the frontend will use the same origin so requests to `/api/...` go to the backend served by the same domain (works behind Nginx).
# REACT_APP_API_BASE_URL=
